<ol class="post-list">
  {{#each posts as |post|}}
    <li>
      <article class="post">
        <div class="title">
          <p>{{post.title}}</p>
          <span>(This is the post)</span>
        </div>

      </article>
    </li>
  {{/each}}
</ol>

